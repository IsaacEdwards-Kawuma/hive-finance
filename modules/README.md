# HiveStack IntelliCash – Module Directory

Place app-store modules here. Each module must follow this structure:

```
modules/
  {VendorName}_{ModuleName}/
    module.json       # Required manifest
    Providers/Main.php
    Routes/admin.php
    ...
```

See the technical specification for the full `module.json` schema and directory layout.
